import * as vscode from 'vscode';
import {TsResolveFile} from '../models/TsResolveFile';

export class TsResolveFileParser {
    public parse(filePath: string | vscode.Uri): TsResolveFile {
        return null;
    }
}
